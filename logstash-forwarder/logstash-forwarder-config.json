{
  "network": {
    "servers": [ "logstash:5000" ],
    "timeout": 15,
    "ssl ca": "/etc/pki/tls/certs/logstash-forwarder.crt"
  },
  "files": [
    {
      "paths": [
        "/var/log/app/*.log",
        "/var/log/icinga2/*.log",
        "/var/log/mongo-graphite/*.log"
       ]
    }
   ]
}