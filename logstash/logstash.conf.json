input {
  lumberjack {
    port => 5000
    type => "logs"
    ssl_certificate => "/etc/pki/tls/certs/logstash-forwarder.crt"
    ssl_key => "/etc/pki/tls/priv/logstash-forwarder.key"
  }
}

output {
  elasticsearch {
    host => "es"
    port => 9200
  }
  stdout { codec => rubydebug }
}