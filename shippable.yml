language: java

jdk:
   - oraclejdk8
script:
  - npm install bower
  - cd fb-web
  - bower install
  - cd ..
  - ./gradlew clean build
before_install:
  - source ~/.rvm/scripts/rvm && rvm use 2.0.0
  - shippable_retry gem install compass
#notifications:
#  email: false
